15.01.19:

HCO - für fetch:
TODO: Componenten, zu finden unter HCO-extra.js, müssen generischer gestaltet werden. - so nur für Filmtitel funktional.
     z.B:  {this.props.swapiFilmTitle.map(filmTitel => (
                        <option key={filmTitel.title}>{filmTitel.title}</option>
                    ))}
     ergo: Überlegen, ob es möglich ist, das anders zu gestalten. Die HCO muss flexiebler werden

16.01.19:
HCO - alleine sollte  generisch sein- die darauffolgenden Komponenten nicht!
Die Komponenten sind vorbereitet: In selection-bar integrieren und der swapiAPI übergeben: falls funktioniert, dann die Loactiuon und den Path der Dateien ändern.

17.01.19:
Refacturierung: this.Wrapper.Film HOC eingebunden: onChange kommt nicht bei Textfilterung an!!

21.01.19: In select-film ist anscheinend kein eventhandler vorhanden, der das onCHange Event abzufangen: 
betstätigt: es fehlte einfach this.props.
Selected-Film Componente abgeschlossen:
Nächster Schritt: Selected Person Componente der HOC übergeben: HOC generischer machen.

... schauen, wie man testgetrieben das sekect-person weiter unterscgucht..

22.01.19: Test - Driven : shallow testing funktioniert nur, wenn die "Untercomponente" in einer eigenen Datei ist.